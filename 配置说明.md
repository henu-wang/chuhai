# ⚡ 重要：避免冲突的配置说明

## 🎯 问题已解决！

为了让这个MCP服务器能够与你现有的FMP MCP共存，我已经做了以下更新：

### ✅ 更改内容

1. **服务器名称已更改**
   - 从：`fmp-mcp-server`
   - 改为：`fmp-chuhai-server`

2. **Claude Desktop配置键名已更改**
   - 从：`"fmp"`
   - 改为：`"fmp-chuhai"`

3. **所有文档已更新**
   - ✅ README.md
   - ✅ INSTALL.md
   - ✅ QUICKSTART.md
   - ✅ DEPLOYMENT_GUIDE.md
   - ✅ setup.sh
   - ✅ setup.ps1
   - ✅ claude_config_example.json

4. **新增文档**
   - ✅ COEXISTENCE.md - 详细的共存指南

---

## 📝 现在如何配置

### 情况1：你只安装这个Chuhai版本

直接使用这个配置：

```json
{
  "mcpServers": {
    "fmp-chuhai": {
      "command": "node",
      "args": ["/你的路径/chuhai/dist/index.js"],
      "env": {
        "FMP_API_KEY": "N1TAoz9ns6c2nqxgTeD8p1CPqqngAIdN"
      }
    }
  }
}
```

### 情况2：你已经有其他FMP MCP（推荐）

**两个MCP可以完美共存！** 配置应该是这样：

```json
{
  "mcpServers": {
    "fmp": {
      "command": "...",
      "args": ["...你原有的FMP MCP配置..."]
    },
    "fmp-chuhai": {
      "command": "node",
      "args": ["/你的路径/chuhai/dist/index.js"],
      "env": {
        "FMP_API_KEY": "N1TAoz9ns6c2nqxgTeD8p1CPqqngAIdN"
      }
    }
  }
}
```

**关键点**：
- ✅ 保留你原有的 `"fmp"` 配置
- ✅ 在同一个 `"mcpServers"` 对象中添加 `"fmp-chuhai"` 配置
- ✅ 用逗号分隔不同的服务器配置
- ✅ 确保JSON格式正确

---

## 🚀 快速部署步骤

### 步骤1：克隆并安装（在本地电脑）

```bash
git clone https://github.com/henu-wang/chuhai.git
cd chuhai
```

### 步骤2：运行安装脚本

**MacOS/Linux:**
```bash
chmod +x setup.sh
./setup.sh
```

**Windows:**
```powershell
.\setup.ps1
```

脚本会自动显示正确的配置（已经是 `fmp-chuhai` 了）。

### 步骤3：配置Claude Desktop

找到配置文件：
- **MacOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
- **Linux**: `~/.config/Claude/claude_desktop_config.json`

**如果文件不存在，创建它。**

**如果文件已存在且有其他MCP：**

打开文件，在 `"mcpServers"` 中**添加**新配置（不要删除原有的）：

```json
{
  "mcpServers": {
    "现有的配置1": { ... },
    "现有的配置2": { ... },
    "fmp-chuhai": {
      "command": "node",
      "args": ["/你的完整路径/chuhai/dist/index.js"],
      "env": {
        "FMP_API_KEY": "N1TAoz9ns6c2nqxgTeD8p1CPqqngAIdN"
      }
    }
  }
}
```

### 步骤4：重启Claude Desktop

**完全退出**并重新启动（不是最小化）。

---

## ✅ 验证部署

在Claude Desktop中输入：

```
请帮我查询苹果公司(AAPL)的股价
```

如果成功，Claude会调用fmp-chuhai的工具返回数据。

你也可以明确指定：

```
使用fmp-chuhai获取特斯拉的公司信息
```

---

## 🔍 如何区分两个FMP MCP

### 查看工具数量
- **Chuhai版本**: 153个工具
- **其他版本**: 可能更少

### 查看启动日志
Chuhai版本在启动时会显示：
```
FMP Chuhai MCP Server running on stdio
Server Name: fmp-chuhai-server (区分于其他FMP MCP)
Loaded 153 tools
```

### 在Claude中明确指定
你可以告诉Claude使用哪个服务器：
- "用fmp-chuhai查询..."
- "使用chuhai版本获取..."
- "用出海版本的FMP工具..."

---

## 📚 详细文档

查看 **[COEXISTENCE.md](./COEXISTENCE.md)** 获取：
- 完整的共存配置指南
- 故障排除技巧
- 高级配置选项
- 使用技巧

---

## 🎁 Chuhai版本的独特优势

即使你已经有FMP MCP，Chuhai版本仍然值得安装：

1. ✅ **完整覆盖** - 153个工具，可能是最全面的FMP实现
2. ✅ **最新API** - 支持2025年最新的FMP端点
3. ✅ **独家功能**：
   - 参议院股票交易数据
   - ESG评分详细数据
   - 批量数据端点
   - 完整的技术指标
4. ✅ **中文文档** - 完整的中文说明和注释
5. ✅ **持续更新** - 活跃维护和功能更新

---

## 💡 专业提示

### 同时使用两个MCP的策略

1. **快速查询** → 使用轻量的FMP MCP
2. **深度分析** → 使用Chuhai完整版
3. **特殊功能**（ESG、参议院交易等）→ 使用Chuhai

### 配置管理

1. **备份配置**：
   ```bash
   cp ~/Library/Application\ Support/Claude/claude_desktop_config.json ~/claude_config_backup.json
   ```

2. **使用JSON验证器**：
   - 在线：https://jsonlint.com/
   - VS Code：内置JSON验证

3. **查看日志**：
   ```bash
   # MacOS/Linux
   tail -f ~/.config/Claude/logs/mcp-server-*.log
   ```

---

## 🆘 遇到问题？

1. **查看文档**
   - [COEXISTENCE.md](./COEXISTENCE.md) - 共存指南
   - [INSTALL.md](./INSTALL.md) - 安装和故障排除
   - [QUICKSTART.md](./QUICKSTART.md) - 快速开始

2. **验证配置**
   ```bash
   node /你的路径/chuhai/dist/index.js
   ```
   应该看到启动信息。

3. **检查JSON格式**
   确保：
   - 括号配对正确
   - 逗号使用正确
   - 路径是绝对路径
   - 引号是英文引号

---

## 🎉 总结

✅ **服务器已重命名**为 `fmp-chuhai`，不会与现有FMP MCP冲突
✅ **所有文档已更新**，跟着新的配置示例操作即可
✅ **新增共存指南** COEXISTENCE.md，详细说明多服务器配置
✅ **代码已推送**到GitHub，可以直接克隆使用

**现在就去你的本地电脑部署吧！两个FMP MCP可以完美共存！** 🚀

---

**出海！一定要出海！** ⛵
